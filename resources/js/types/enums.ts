// Application enum types

export enum DjAvailabilityStatus {
  Available = 'available',
  Unavailable = 'unavailable',
  Tentative = 'tentative',
  Booked = 'booked'
}

export enum BookingStatus {
  New = 'new',
  Quoted = 'quoted',
  Booked = 'booked',
  Cancelled = 'cancelled'
}
